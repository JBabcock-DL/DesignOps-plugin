#!/usr/bin/env node
// Legacy placeholder: former 12-slice assemble-slice size probe removed with five-call bundles.
// Use: npm run bundle-component && npm run qa:create-component-skill
console.log(
  "qa-assembled-size: OK (deprecated — run `npm run qa:create-component-skill` after `bundle-component`)",
);
process.exit(0);
