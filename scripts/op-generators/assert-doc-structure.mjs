/**
 * Declarative §6.9 doc-frame invariants (future: consumed by op-interpreter assertDocStructure).
 * Full structure checks run in draw-engine `__ccDocFinalizeAndReturn` / delegated step6 min.
 */
export const DOC_STRUCTURE_INVARIANTS_V1 = {
  version: 1,
  /** Placeholder — expand with node name patterns + min child counts */
  rules: [],
};
