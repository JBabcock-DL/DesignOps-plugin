#!/usr/bin/env node
// Stub: compare legacy min bundles vs op pipeline output (see plan Phase 6).
// Figma fixture diff not automated in repo CI yet.
console.log('qa:visual-diff: OK (no fixture diff configured — implement when op pipeline covers all 10 slugs)');
process.exit(0);
